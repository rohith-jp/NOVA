# NOVA Celery Worker — Python dependencies
# The worker also imports from the backend app package (app.core.*, app.db.*),
# so it installs the same core deps as the backend.

fastapi==0.141.1
uvicorn[standard]==0.52.4
python-dotenv==1.0.1

# Database
supabase==2.31.0
psycopg2-binary==2.9.10

# Celery + Redis
celery==5.6.3
redis==8.1.0

# AI — Google Gemini (used by planner/gemini shared service)
google-genai==1.25.0

# Security & auth
argon2-cffi==25.1.0
cryptography==50.0.0
python-jose[cryptography]==3.5.0
PyJWT==2.13.0

# Memory / embeddings
sentence-transformers==6.0.0

# HTTP client
httpx==0.28.1

# Browser automation
playwright==1.62.0

# Anthropic Claude
anthropic==0.125.0
