# NOVA Backend — Python dependencies
# For reproducible deploys, Railway (and local venvs) should install from this file.
# Pin versions to avoid silent breakage on redeploy.

fastapi==0.141.1
uvicorn[standard]==0.52.4
python-dotenv==1.0.1

# Database
supabase==2.31.0
psycopg2-binary==2.9.10

# Celery + Redis (broker/backend)
celery==5.6.3
redis==8.1.0

# AI — Google Gemini
google-genai==1.25.0

# AI — Anthropic Claude
anthropic==0.125.0

# Security & auth
argon2-cffi==25.1.0
cryptography==50.0.0
python-jose[cryptography]==3.5.0
PyJWT==2.13.0

# Memory / embeddings
sentence-transformers==6.0.0

# HTTP client (used by TTS and STT services)
httpx==0.28.1

# Browser automation (Celery task)
playwright==1.62.0

# Testing (used in CI — safe to install in prod too, no runtime cost)
pytest==8.3.5
pytest-asyncio==0.24.0
