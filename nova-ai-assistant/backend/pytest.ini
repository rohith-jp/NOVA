[pytest]
# Test discovery
testpaths = app
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output
addopts =
    --tb=short
    --strict-markers
    -q

# Custom markers
markers =
    integration: Tests that require live external services (skip in CI)
    unit: Pure unit tests with no external dependencies
